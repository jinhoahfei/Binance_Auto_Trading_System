# 장시간 운용과 시세 장애 복구

## 복구 정책

시세 중복·지연 입력은 최신 봉을 되돌리지 않는다. 같은 시각의 모순된 입력은 임의로 선택하지 않고 재동기화한다. 마감봉과 다음 봉은 도착 순서가 바뀌어도 원자적으로 반영하며, 시간 정합성이 아직 맞지 않는 입력은 주기별 최대 두 봉까지만 보류한다. 경계를 가로지른 REST 조회는 최대 세 번 다시 읽고, 실패하면 단일 복구 작업이 1/2/4/8/16/30초 간격으로 재시도한다.

시세 복구 자체와 거래 재개는 별개다. 장애 직전 실행 중이던 세션만 기존 계좌 재연결·동일 주문 ID 조회·체결 이력 대조·잔액 검증을 통과한 뒤 재개한다. 보유 포지션의 수량·전략 소유권·청산 기준·진입 시각은 보존한다. 중단 전 시장 평가와 전략 타이머는 폐기하고, 다음 시장 입력에서 연속 유지 조건을 다시 측정한다. 확정 주문 결과와 사용자 명령은 보존한다.

사용자 중지·유효한 프로그램 종료 요청·강제 중지·프로세스 오류·미확인 주문·계좌 불일치는 자동 재개하지 않는다. 검증 실패는 기존 거래 차단을 유지하며 신규 주문으로 복구하지 않는다. 실제 주문 한도와 매매 전략의 조건은 변경하지 않는다.

## 표시 및 진단

기존 거래 상태 DTO에 선택적 `recovery` 필드를 추가했다. `phase`는 `idle`, `market`, `account_orders`, `blocked`, `resumed`이며 시작 시각, 시도 횟수, 차단 사유, 마지막 시세 입력·전략 평가 시각, 60초 초과 여부를 포함한다. 구버전 응답은 기존 방식으로 표시한다.

실행 중 전략 평가가 60초 이상 멈추면 시세 재동기화를 요청한다. 정상적인 조건 대기는 전략 평가 시각이 갱신되므로 중단으로 취급하지 않는다. 주기적인 상태 게시로 복구 지연을 화면에도 반영한다. `market_input_disposition`, `market_boundary_waiting`, `strategy_evaluation_stalled`, `trading_session_resumed` 로그로 입력 폐기와 복구 진행을 추적한다.

시장 평가 큐는 증가하는 시장 버전으로 중복을 제거한다. 따라서 장시간 실행의 모든 시장 이벤트 ID를 메모리에 영구 보관하지 않는다. 주문·사용자 이벤트의 중복 방지는 기존 방식으로 유지한다.

## 검증 방법

프로젝트 루트에서 실행한다. 실제 주문은 기본 비활성화한다.

```sh
cd backend
BINANCE_RUN_TESTNET=0 BINANCE_RUN_TESTNET_ORDERS=0 BINANCE_RUN_PHASE13_PUBLIC_CASE2=0 PYTHONPATH=src .venv/bin/python -m unittest discover -s tests -p 'test_*.py'
```

UI는 `UI` 디렉터리에서 `pnpm typecheck`, `pnpm test`를 실행한다. 로컬 통신 테스트에는 루프백 포트 생성 권한이 필요하다.

추가 검증에는 4시간 경계 24개·자정 경계 720개 도착 순열, 중복·역순·동일 시각의 모순 및 마감 전환, 무포지션/보유 포지션 각각 100회 복구, 중지·프로세스 장애 보존, 이력 불일치 차단, 가상 72시간(2초 간격 129,600건)의 큐 중복 제거·메모리 검사가 포함된다. 가상 72시간 검사는 큐 검증이며 72시간 전체 거래소·전략 통합 운용의 증거로 해석하지 않는다.

## 실제 시세 48시간 점검

```sh
backend/.venv/bin/python scripts/market_stability_soak.py --hours 48 --output Log_History/market_stability_48h_NEW_RUN
```

이 점검은 실제 공개 REST·WebSocket과 운영 시세 처리기·지표 계산기를 사용한다. 로컬 API 키나 `.env`를 읽지 않으며 계좌·주문 인터페이스를 제공하지 않는다. 기존 앱을 중지하거나 재시작하지 않는다. API 계좌·실제 주문 복구 검증은 별도 Testnet 인증 환경에서 수행해야 한다.

10초 간격 `metrics.jsonl`과 `status.json`, 분할 진단 로그를 보존한다. 실제 경과 시간, 마지막 평가 이후 시간, 재연결 오류, 이벤트 수, 스레드 수, 최대 메모리 사용량을 기록한다. `completed_needs_review`는 시간 측정 완료를 뜻하며 안정성 합격을 자동 선언하지 않는다. 재연결 지연과 리소스 추이를 검토하고, 48시간 경과 전에는 장시간 안정성 검증 완료로 보고하지 않는다.

## 2026-09-10 구현 검증 기록

- 앱 Python 3.11 환경: 백엔드 1,125개 실행, 10개 건너뜀, 나머지 통과.
- UI: 타입 검사 통과, 49개 파일의 테스트 499개 통과.
- 결과 원문: `Log_History/stability_verification_20260910/`.
- 실제 공개 시세 48시간 점검: 한국 시간 2026-09-10 13:58:12 시작, PID 61459. 상태와 누적 자료는 `Log_History/market_stability_48h_20260910/`에 저장된다.
- 시작 직후 실제 시세 입력과 운영 지표 계산을 확인했다. 아직 48시간 검증 완료 또는 합격을 뜻하지 않는다.
- 전용 Testnet 인증 정보가 없어 실제 Testnet 주문 검증은 실행하지 않았다. 실거래 주문도 실행하지 않았다.
- Codex의 정기 후속 점검은 자동 승인 검토에서 별도 사용자 승인 필요로 거부되어 생성되지 않았다. 점검 프로세스 자체는 독립적으로 계속 실행되며 종료 상태와 기록을 남긴다.

## 장애 발생 후 로그 분석 보완

- `market_input_disposition`: 기존 시각·봉 구간·마감 상태를 유지하고, 충돌이면 `previous_kline`과 `received_kline`의 정규화된 OHLCV까지 기록한다. 늦게 도착한 마감 봉끼리의 충돌도 포함한다. 중복·역순 폐기에는 OHLCV를 반복 저장하지 않는다.
- `market_boundary_waiting`: 필수 입력 대기에는 경계 시각, `missing_sources`와 `received_sources`를 기록한다. 시각 검증 대기에는 고정된 `validation_rule`, 보류 봉, 주기별 최종 후보 봉 및 이전 게시 시각을 남긴다. 이전 게시 시각은 실패한 조회의 검증 시각을 뜻하지 않는다.
- `trading_session_resume_blocked`: 종료 요청·다른 위험 차단·계좌/주문 검증 실패·미확인 주문·포지션 이력 불일치가 재개를 막은 순간 기록한다. 당시 복구 단계·시도 횟수·개별 차단 플래그·세션 및 시장 식별자를 보존하며, 예외가 있으면 원문 대신 원인 타입과 내부 함수·행을 기록한다.

`backend/.venv/bin/python scripts/verify_runtime_logging.py`는 외부 연결 없이 합성 장애를 재현하고 실제 저장된 로그를 검증한다. 추가 시나리오는 과거 장애의 원본 payload를 복원한 것이 아니다. 원시 응답과 인증 정보는 기록하지 않는다. 변경은 다음 프로그램 시작부터 적용되며 이미 실행 중인 프로세스에는 소급 적용되지 않는다. 이 기록 보완은 48시간 연속 운용 합격을 뜻하지 않는다.

로그 보완 검증: 파일 저장 검증 20개 통과, 백엔드 전체 1,129개 실행(10개 건너뜀), 실패 없음. 검증 원문은 `Log_History/logging_enhancement_verification_20260910/`에 보존한다.

## 2026-09-13 UI–백엔드 연결과 종료 복구

전체 상태를 다시 읽는 중 일시적 오류가 발생해도 UI 인증 정보를 폐기하지 않고 단일 작업으로 재시도한다. 재연결한 snapshot과 정상 이벤트 수신까지 확인한 뒤 화면을 online으로 복원한다. UI 연결만 끊겼을 때 정상 백엔드의 자동매매는 유지하며 화면 명령은 복구까지 차단한다. 종료는 최신 상태 확인·중지·정상 종료를 거치고, 치명적 UI 오류에서도 같은 백엔드의 좁은 종료 API를 사용할 수 있다.

UI 최초 오류를 백엔드 통신과 독립된 native 로그에 저장한다. 발생 단계, 고정 오류 코드·타입, 검증 필드, request/incident/session 식별자, HTTP 상태·소요 시간, 소켓 close code, 마지막 정상 수신, 재시도 및 저장 유실 수를 남긴다. 자세한 원인·수정·분석 방법은 `Design/Validation/Backend_Connection_Recovery_2026-09-13.md`에 정리했다.

검증: 백엔드 1,159개 실행(10개 제외), UI 531개, native 51개 통과. 배포용 macOS 앱 빌드 완료. 주문 없는 실제 loopback 통신 24시간 점검은 `Log_History/backend_connection_soak_24h_20260913_final/status.json`에서 진행 상태를 확인한다. 이는 가짜 계좌 runtime과 Node 진단 파일 sink를 사용하는 통신 통합 검사이며 실제 WebView·거래소·실주문 장시간 운용을 대체하지 않는다. 24시간 완료 여부는 해당 기록으로 별도 확인한다.

## 2026-09-14 UI–백엔드 24시간 점검 분석

2026-09-13 01:23:57~2026-09-14 01:23:57 KST에 최종 점검을 완료했다. 장애 95회 모두 복구됐고 중앙 복구 시간은 4.074초, 최장은 5.022초였다. UI 진단 4,546건·backend 진단 3,832건은 sequence 유실 없이 보존됐다. 소켓은 최대 1개, backend thread는 준비 후 4개로 유지됐다.

검증한 통신 복구·진단 범위 내 통과로 판정했다. Node heap 증가 후 감소를 관찰했지만 GC trace와 heap snapshot이 없어 누수 부재를 확정하지 않는다. 실제 WebView·Tauri 저장 전체 경로·거래소·매매는 장시간 검증 범위 밖이다. 자세한 분석과 원본 연결은 `Design/Validation/Backend_Connection_Soak_Result_2026-09-14.md`에 기록했다. 원본 상태 파일의 `completed_needs_review`는 보존하고 분석 판정은 별도 파일에 저장했다.
